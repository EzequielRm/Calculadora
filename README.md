# Calculadora

Proyecto simple de calculadora web con `index.html`, `index.js` y `style.css`.

Instrucciones:

- Abrir `index.html` en el navegador para usar la calculadora.
- O ejecutar un servidor local (recomendado) desde la carpeta del proyecto:

Licencia: código propio.
